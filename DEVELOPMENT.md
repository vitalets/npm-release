# Development

Install dependencies and run the tests before submitting changes:

```bash
npm ci
npm test
```

## Releasing

1. Run `release` action on GitHub
2. Select `major`
3. After release, pull the changes and update docs.
